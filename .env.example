# Supabase — RescueGPS NavMate
#
# OPTIONAL. The app ships with these values compiled in (see src/lib/supabase.ts)
# so a fresh clone and a fresh Vercel project both run with no configuration.
# Set them only to point a build at a different Supabase project — a fork, or a
# staging environment.
#
# Both values are publishable and safe in the client bundle; row level security
# is what protects the data. Never put a service-role/secret key here.

VITE_SUPABASE_URL=https://puzwcsrtqtbutypzozvu.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_epPlnaBqhZw7GAFxYcCxag_PqOh77-0
